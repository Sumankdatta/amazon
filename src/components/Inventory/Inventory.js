import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Inventory = () => {
    // const meals=useLoaderData()
  
    // console.log(meals)
    return (
        <div>
            {/* <h2>inventory {meals.length}</h2> */}
        </div>
    );
};

export default Inventory;